build:
  python_version: "3.10"
  system_packages:
    - git

predict:
  input:
    prompt:
      type: string
      description: Text prompt
    width:
      type: integer
      default: 512
    height:
      type: integer
      default: 512
  output:
    type: image
  command: "python predict.py"
